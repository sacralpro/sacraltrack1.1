function repost(req, res) {
    // логика создания репоста
  }
  
  function getReposts(req, res) {
    // возвращает репосты пользователя  
  }
  
  module.exports.repost = repost;
  module.exports.getReposts = getReposts;