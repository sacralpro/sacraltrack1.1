// utils/recommendations.js

const User = require('../backend/controllers/models/User');

function getRecommendations(userId) {
  // используем модель User
}

const Track = require('../backend/controllers/models/Track');

function getRecommendedTracks(userId) {
  // используем модель Track 
}